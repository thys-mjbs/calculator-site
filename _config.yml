title: "Calculator Site"

url: "https://snapcalc.site"
baseurl: ""

plugins:
  - jekyll-sitemap

# Keep Jekyll behaviour minimal so your static HTML keeps working.
# No theme, no layouts, no collections are defined here.
# GitHub Pages will run Jekyll and auto-generate:
#   /sitemap.xml
#
# Ensure there is NO `.nojekyll` file in the repo root.
