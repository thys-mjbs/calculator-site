title: "Calculator Site"

url: "https://thys-mjbs.github.io"
baseurl: "/calculator-site"

plugins:
  - jekyll-sitemap

# Keep Jekyll behaviour minimal so your static HTML keeps working.
# No theme, no layouts, no collections are defined here.
# GitHub Pages will run Jekyll, see this config, and auto-generate:
#   /calculator-site/sitemap.xml
#
# You must ensure there is NO `.nojekyll` file in the repo root.
